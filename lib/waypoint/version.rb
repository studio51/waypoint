# frozen_string_literal: true

module Waypoint

  # The engine's released version, shown in the dashboard footer so an operator
  # can tell which build recorded a run.
  #
  VERSION = "0.1.0".freeze
end
